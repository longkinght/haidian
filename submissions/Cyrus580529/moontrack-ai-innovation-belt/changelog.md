# 方案迭代记录

本文件记录本方案从初版到当前版本的实际改动，包括**被推翻的结论**与**被删除的内容**。删除比新增更能说明这份方案的作业方式，因此一并保留。

## v0.11 - 2026-08-27

### 改动摘要

- **收窄「全部结论包内可复算」这句话。**摘要此前这样写,但 `sources.json` 里 `osm-footways-walking-network` 自己写明「本数据不进提交图层」,而正文引用的 `analysis_index.py`、`analysis_service.py`、`analysis_policy.py` 都不是提交包文件。也就是说 8.61% 步行网可达、6.5 倍增益这批反事实指标,实际是「按登记方法可复算」,不是「包内可复算」。现按两类分开表述:几何、用地、建筑与慢行网连通性结论可用包内 `visual/assets/verify-geometry.js`、`verify-network.js` 直接复算(Node 标准库,不依赖 GIS 库,不一致退出码为 1,本轮实测两者均为 0);步行网反事实须先按 `sources.json` 登记的 Overpass 查询重取输入。中英文摘要、正文、A3/A0 页脚、可达率图注与 HTML 同步。
- **统一跨文件的版本化数量。**以当前 `metrics.json` 与项目清单为唯一口径,修正四处陈旧统计:`proposal.md` 与 `proposal.en.md` 称指标「共23项」——实为 58 项、57 项 known(唯一 unknown 为 `floor_area_ratio`);`design_depth_matrix.json` 称「共34项、33项 known」;`sources.json` 与 `design_depth_matrix.json` 三处称邻河车道「26段」——实为 42 段(`cycleway_near_river_300m_segments`,总数 421 段不变);`design_depth_matrix.json` 称更新清单「JZ-01至JZ-07」——正文已有 JZ-08。图件生成脚本里的「50 项已知指标」同步改为 57,`metrics-evidence` 与 `machine-accessibility` 三张图及 A3/A0 已重出。

### 采纳反馈

- PR #4080 第二轮 AI 评审(七维加权 91.0/100,较上轮 88.0 提升)提出两项阻断:复算声明的证据边界不清;跨文件统计数字冲突。两项均核实属实,本轮全部修复。
- 上轮两项阻断已关闭:表达完整度由 3/5 升至 4/5(中文 HTML 方框已消除),`data_confidence` 冲突已解决。

### 暂未采纳或待复核事项

- 未把 `analysis_*.py` 与冻结的 OSM 步行网快照纳入提交包。提交包的定位是设计成果而非代码仓库,且步行网输入按 `sources.json` 登记口径本就不作提交图层;选择把话说准,而不是为使「包内可复算」成立而扩大包体。
- 可实施性维持 4/5,原因是 0/5 适老可达、控规与权属未公开——属组织方数据缺口,rubric 明定此类缺口不因此扣分。

## v0.10 - 2026-08-27

### 改动摘要

- **四份离线 HTML 补上嵌入中文字体。**页面此前只在 `font-family` 里列出 `Hiragino Sans GB`、`PingFang SC`、`Microsoft YaHei` 等系统字体,自己不带字形。这与 v0.9 修掉的 PDF 缺陷是同一个失效模式,当时只修了 PDF 那一半:在没有 CJK 字体的无头审查环境里,中文渲染成方框。现按每份页面实际用到的字符,从同一套 Noto Sans SC(OFL 1.1)切出 400/700 两个 woff2 子集,base64 内联为 `@font-face`,并把该字族插到原有字体栈最前,原栈保留作回退。全程无远程请求。
- 体积代价:`report/proposal.html` 181→629 KB,`visual/index.html` 37→213 KB,两份英文页各增 14—42 KB。
- 已逐字符核验:四份页面的可渲染字符(含 JS 运行时生成的标签文字)全部落在各自子集的 cmap 内,未覆盖 CJK 字符为 0。另做隔离验证——把页面字体栈里除嵌入字族外的全部回退项删掉后用无头 Chrome 截图,中文仍完整可读,证明字形来自嵌入子集而非本机系统字体。
- **`manifest.validation_claim.data_confidence` 由 `high` 改为 `mixed_provisional_and_conceptual`。**`metrics.json` 的逐项分布是 high 8 / medium 35 / low 14 / unknown 1,包级声称 high 与之冲突,会让机器消费者误读总体证据等级。取该枚举值而非 `medium`,是因为它本身就表示证据等级不均一,正对应本包的实际状况:provisional 边界 + OSM 派生指标 + 概念性方案内容。逐项分布、判定理由与「不得用于覆盖单项等级」写进 `validation_claim.extensions` 的 `x-data-confidence-note`——schema 对 0.2.x 把 `validation_claim` 锁为五个键,`extensions` 是其规定的扩展点。
- `report/proposal.html` 与 `report/proposal.en.html` 按当前 `proposal.md` 重新渲染(v0.9 改过参考资料章节,HTML 此前是旧版)。`report/copyright_statement.md` 的 Fonts 一节补入 HTML 字体行,并改掉此前把"页面不带字体"当作合规优点的表述。

### 采纳反馈

- PR #4080 的 AI 评审(七维加权 88.0/100)提出两项阻断:中文 HTML 大量文字显示为方框;`data_confidence=high` 与逐项等级冲突。两项均核实属实,本轮全部修复。
- 表达完整度一维此前评 3/5,原因即中文 HTML 不可读;其余六维为 5/5/5/4/5/4。

### 暂未采纳或待复核事项

- 英文 HTML 也嵌了同一子集:两份英文页各含 72 个和 1 个中文字符(专有名词),不嵌同样会出现方框。
- PDF 与 HTML 现在共用同一套字体来源,但子集各自独立生成,不共享文件;提交包内仍不含独立字体二进制。

## v0.9 - 2026-08-26

### 改动摘要

- **中文图纸字体改为嵌入。**此前两份中文 PDF 使用 reportlab 内置 CID 字体 `STSong-Light`,那是**不嵌入**的 CIDFontType0:PDF 里只写字体名、不写字形,由阅读器用自带的 Adobe 亚洲字体包或系统 CJK 回退来补。现改为 Noto Sans SC(OFL 1.1)以 TrueType 子集嵌入,`drawings/a3-booklet.pdf` 与 `drawings/a0-boards.pdf` 的字体描述符现含 `FontFile2`。
- 未使用系统里的 `Songti.ttc` / `STHeiti*.ttc` / `Hiragino Sans GB.ttc`:那些是 Apple 授权字体,嵌入进公开发布的 PDF 不在其许可范围内。字体来源、许可与实例化方式记在 `report/copyright_statement.md` 的 Fonts 一节;字体二进制不作为独立文件进入提交包。
- 顺带得到一个此前没有的效果:中文粗体过去与正文同字重(`CNB` 就是 `CN`),现在是 700 字重,标题层级更清楚。中文两份 PDF 与 manifest 校验值同步重出;英文两份内容未变(正文全部为拉丁字母),按原字节保留,不制造无谓的二进制改动。
- **补齐四份派生 CSV 的来源与公开性说明。**`project_scope_summary.csv`、`agent_task_requirements.csv`、`source_use_matrix.csv`、`missing_data_checklist.csv` 此前只在参考资料里列了文件名,未登记上游来源,`score_submission.py` 的「公开资料引用」一项因此为 needs-work。现已在 `sources.json` 中登记为 `repository_processed_reference`,以 `derived_from` 字段引用 `data/source_registry.json` 中登记的上游 `source_id`,并标注 `public_status`;正文双语参考资料条目补上派生关系与 `[source:*]` 标签。

### 采纳反馈

- 外部复审指出中文 PDF 未嵌入字体。经复核,旧版确实依赖阅读器的 CJK 字体替代;两份中文 PDF 在本机 macOS 环境显示完整,但在缺少相应字体回退的 Linux Poppler 环境中可出现缺字或空白。为保证评审环境中的跨平台一致性,改为嵌入 Noto Sans SC。
- 同一复审指出派生 CSV 引用不完整。核对后确认,并发现自检里还有第五条未匹配条目(机器索引行),一并补上;「公开资料引用」现为 8 pass / 0 needs-work。

### 暂未采纳或待复核事项

- 英文 A3/A0 仍不嵌入字体:正文使用 Helvetica/Helvetica-Bold,属于 PDF 标准 14 字体,常规合规阅读器可提供替代,按本次交付标准无需嵌入。
- 组织方校验器对 PDF 只查空文件,不查字体嵌入;本轮修复属方案自定质量线,不是门槛要求。

## v0.8 - 2026-08-26

### 改动摘要

- **拉丁字标改用按书面要求生成的定制字形**：几何无衬线、低笔画对比、圆形字碗、水平截线，T 横杠沿长度开一道窄缝取自轨道双线。**按生成原样使用**——不描摹、不重上色、不重切字形；承载面板底色取自生成图自身，接缝不可见。
- 封面、识别系统图版、A3 识别页同步改用该字标；`report/copyright_statement.md` 中把字标与徽章一并声明为 AI 生成资产。
- **只提供深底一版**：生成的字标本身是深底，浅底版无法在不改动像素的前提下得到，因此移除 `moontrack-wordmark-light*.png`。

### 采纳反馈

- v0.7 的字标是用系统字体排出来的，那不是字形设计。本轮改为生成定制字形。
- 过程中一度对生成图做了两处手工处理（裁齐 T 横杠上半条、阈值抠像后重上色），已全部撤销：手工抠出的 alpha 边缘比原图抗锯齿粗糙，得不偿失。**生成资产一律按原样使用**，这条现在是本方案的处理规则。

### 暂未采纳或待复核事项

- 拉丁字标与徽章均为 AI 生成，不描绘任何既有场所，声明见版权文件。
- 浅底场合目前无可用字标；如需，须另行生成，不由脚本合成。

## v0.7 - 2026-08-25

### 改动摘要

- **放弃图形标志，改用字标 + 徽章两级。**先后做过四种图形符号（河道曲线、月相、人字切口、双轨折线），共同问题是读者看不懂——需要解释的标志就是没做成。现在标识主形是字标「月轨 MoonTrack」，用于一切场合尤其小尺寸；月洞门徽章用于封面、图纸扉页与展板，128 像素以下不可辨识故不作小尺寸标识。
- 封面重做：月洞门徽章 + 字标 + 三个实测读数（0/5、0.69%、6.5×）。
- `assets/media/` 更新：移除 `moontrack-mark*.png`，新增徽章与四个字标文件。
- 识别系统图版重写为"字标 / 徽章 / 色彩 / 次级母题 / 禁用"五段；A3 识别系统页同步。

### 采纳反馈

- 前一版标志（月轮 + 小月河实测中线）在"与图层同源、可复算"上是成立的，但**辨识度不足且需要解释**。本轮判定：可读性优先于同源性，因此推翻前一版做法。这条推翻记录保留在此，不在正文中掩盖。

### 暂未采纳或待复核事项

- 徽章为 AI 生成的品牌图像，非现场照片，不描绘任何既有场所、任何文保建筑或任何可辨识个人；声明见 `report/copyright_statement.md`。
- 字标与识别系统图版仍由 `make_identity.py` 程序绘制，未使用生成式图像模型。
- "标志曲线取自小月河实测中线"这一论证随前一版一并作废，正文与版权声明均已更新，不保留过时说法。

## v0.6 - 2026-08-25

### 改动摘要

- 新增**五张片区场景图版**：众智园可参观测试沙盒、AI原点社区近校成果转化街、大钟寺四象限步行缝合、京张遗址公园主轴南北贯通、小月河共行走廊一期。与既有三张构件图版同一套版式与调性，图上只出现本方案实测算出的数与正文已明文写下的规则。
- 正文图件 12 → 17 张，A3 文册 15 → 20 页。八张图版统一放大图框（840 → 1000），插图由宽度定尺。
- 图版改为 256 色调色板保存：十张场景图版会把包顶到 40 MiB 上限之外，量化后视觉无损、体积减半，包体 48 → 31 MiB。

### 采纳反馈

- 自审发现图件数量并非短板（11 张，全场中位 5 张，超过 90% 的包），但**构成偏了**：11 张里只有 3 张带画面感且全是构件，没有一张画的是"地方"。场上统计佐证这是普遍空白——分析图 970/970 个包都有，透视效果图仅 49/970、鸟瞰轴测仅 26/970。本版据此补齐五处片区的空间表达。

### 暂未采纳或待复核事项

- 京张遗址公园沿线确有列入文物保护的既有建筑。场景图版在生成阶段即以显式约束排除站房、历史铁路建筑、文保标牌与复原时代构筑物，图上只出现一段普通旧站台边沿；相关声明已写入 `report/copyright_statement.md`。
- 五张插图均为 AI 生成的概念表达，非现场照片，不描绘任何既有场所；图版正面与版权声明中均已标明。

## v0.5 - 2026-08-25

### 改动摘要

- 新增**核心主张一节**：机器可达率与可读的路权。取带内 1,619 个真实目的地，定义与驿站无关的两两口径——任取两个门、一台合规低速设备能互达的比例。实测 **0.69%**（接入率 23.2%），同一批门按步行网算是 8.61%，差 12.5 倍。据此主张这条带最先需要的 AI 基础设施是一层可机读、可撤回的路权数据，而不是又一个沙盒或算力节点。
- 新增 `analysis_index.py` 与 `assets/figures/machine-accessibility*.png`，A3 增至 14 页（新增核心主张页）。
- 新增 `spatial.json`：16 个概念节点／廊道／区域，位置一律文字示意，带数字的条目其数值来自本包实测。
- 摘要重写：从"路权定价"改为直接给出机器可达率与主张。

### 采纳反馈

- 自审认为原有亮点（路权定价）虽然在 950 份摘要中唯一，但落点仍是交通分析，对"AI 创新"这一维回应不足。本版把它推到基础设施主张层面，并给出一个不依赖驿站选址的口径来堵住"换个驿站呢"这一问。

### 暂未采纳或待复核事项

- 0.69% 这一数值继承 OSM 测绘完整性与 8 米吸附容差的限制；两两口径下碎裂按平方进入结果，必须与接入率 23.2% 并读，不得单独引用（A-IDX-001 至 A-IDX-003）。
- 步行网对照值 8.61% 是下界：OSM 人行道测绘零碎，真实连通性只会更好。
- 本方案不主张机器可达率应达到任何目标值，也不主张这层数据由谁建设维护——只给基线与方法。

## v0.4 - 2026-08-25

### 改动摘要

- 新增 `simulation.json` 调度台账：十个真实目的地 × 三种路权状态共 30 条派送任务。**每条任务的成败由实测路权图上的 Dijkstra 判定，不使用模板建议的合成轨迹。**读数为成功 3 条、超预算 5 条、审计完整率 30%，全部在正文叙述并以 `[metric:...]` 引回指标体系。
- 新增“区域创新协同”一节，并附实测底盘：带内轨道站 5 个、近带 800 米内 7 个、带内轨道里程 30,014 米；跨区域门户清河站距带界 3,256 米。旗舰驿站（由极小化最远点算出，输入不含任何轨道数据）落点距六道口站 34 米。
- 新增 `risk.json`，八个维度逐条给出评分、缘由、缓解措施与人工复核节点。
- A3 文册由 7 页扩到 12 页，新增政策杠杆页、调度台账页与三张构件图版页；A0 展板由 2 板扩到 3 板，新增“从诊断到解法”。
- 图纸版式重排：原为左图右文，图受栏宽限制只能占 58% 页高，每页下半幅留白。改为图占上幅、注释分栏在下，同一张图放大约三成。

### 采纳反馈

- 自审发现官方评审维度 `regional_synergy` 点名的北纬社区、未来科学城、怀柔科学城、经开区与京津冀，在 v0.3 全包中出现次数为 0，属整维度缺失，本版补齐。
- 自审发现三张构件图版与政策杠杆图只存在于正文与 HTML，未进入 A3/A0，最强内容没有上主展面，本版补入。

### 暂未采纳或待复核事项

- “北纬社区”在站点资料包与公开渠道均未检索到可引用的定义与边界，因此在协同分工表中留白，不编造联动关系。
- 调度台账审计完整率仅 30%，主因之一是**高校与产业锚点两类目的地本方案未指定人工交接方**。这是真实的运营缺口，本轮无法关闭，如实记入台账。
- C 轮（允许人行道通行）成功数 2/10 是下界而非结论：OSM 步行网测绘零碎，连通性在该数据上证不出来，该轮只作末端距离的证据。

## v0.3 - 2026-08-25

### 改动摘要

- 把旗舰场景从“诊断”推进到“解法”：以分量图上的度量闭包加 MST（Steiner 树 2 近似）求出让五家适老机构进入同一张网的最小连接集 596 米，并以极小化最远点定出驿站位置，最远一家 14.0 分钟。
- 量化路权规则本身的代价：同一批 1,619 个目的地，按非机动车道量末端中位数 249.6 米，按步行网量 82.8 米。政策杠杆表显示提速无效（15→25 km/h 可服务机构零增加），开放人行道换来 6.5 倍可达网络且零工程。
- 补充参数稳健性扫描（吸附容差 4–20 米）与 1,619 目的地尺度检验。
- 新增两个独立复算脚本 `visual/assets/verify-network.js` 与 `verify-geometry.js`，与生成数据的 Python 实现不共享任何代码。

### 采纳反馈

- 自审认为“把诚实作为亮点”不成立：诊断出旗舰场景跑不通却停在那里，对主管部门没有使用价值。本版据此把工作重心从“说明限制”转向“给出代价与顺序”。

### 暂未采纳或待复核事项

- 编写 `verify-network.js` 过程中发现自身几何存在 6 处边界穿越，追溯为 `despeckle` 丢弃了小于 25 平方米的孔洞。已修正为保留全部孔洞。复算脚本因此在提交前就发现了一个真实缺陷。

## v0.2 - 2026-08-25

### 改动摘要

- **删除全部虚构几何。**初版包含一栋 310,807 平方米的“AI研发示范建筑基底”、四块虚构用地和一个 4.59 平方公里的一期多边形，均无数据来源。这与本方案自己写下的“任何无法复算的内容不进入正式结论”直接冲突。
- 以实测数据替换：1,562 栋真实建筑基底、378 块真实用地、7 个由前述图层派生的分期要素。
- 清理正文中 23 段脚手架式说明文字（占正文 15.7% 降至 0.3%），并补写两个此前为空的章节。

### 采纳反馈

- 自审发现虚构几何后即时删除，未等待评审指出。

### 暂未采纳或待复核事项

- OSM 用地覆盖仅 53.6%，残余多边形按代码 16「留白用地」如实登记，不做插值填充。
- `geometry/constraints.geojson` 仍为单个占位要素，阻塞于官方管控条件未公开，已在正文与自检中披露。

## v0.1 - 2026-08-13

### 改动摘要

- 建立 MoonTrack 正式包：总体概念与命名体系、三层范围工作框架、十张 AI 场景卡、指标与来源体系、合规矩阵与双语成果。

### 采纳反馈

- 首版提交，暂无外部反馈。

### 暂未采纳或待复核事项

- 官方控规条件未发布，用地强度、建筑高度、道路红线不具备编制条件，本方案在这些项上不给数字。

## 公开资料与合规说明

全部版本仅使用公开资料：官方公告与面向智能体的开源征集任务书、海淀区统计公报、北京市公开政策文件，以及 OpenStreetMap 众包地理数据（ODbL 1.0）。不包含个人隐私信息、依法不得公开的材料，也不包含未审定的规划控制指标。三张构件图版的底图为 AI 生成的概念表达，非现场照片，不描绘任何既有场所，相关声明见 `report/copyright_statement.md`。
