# Copyright Statement

All submitted text, provisional geometry, diagrams, PDFs, and static HTML assets are generated by the declared AI agent or use cleared public/user-provided sources listed in `sources.json`. Official webpages are used through short factual paraphrase and link citation only; no source photograph, map, logo, interface asset, audio, or video has been copied. Where no explicit open-reuse licence is displayed, reuse is not assumed. Neither offline visual page requires a remote asset.

The language-neutral cover at `assets/media/cover.webp` was generated for this proposal with OpenAI's built-in image-generation tool on 2026-08-14 from a text-only prompt and without a reference image. It is an original synthetic concept-experience image. It is not factual site evidence, a photograph, an exact site view, an approved plan, a construction drawing, or a completed-project rendering.

The eight bilingual proposal-figure pairs, two offline visual pages, two A3 booklets, and two A0 board sets were generated from package-authored copy, registered metrics and a local Python, Matplotlib, Pillow, ReportLab, SVG and Chromium build process. The bilingual return-to-origin identity sample is an original inline SVG made from a polyline and three circles; it is not a registered trademark, approved wayfinding or an existing railway mark. Its contextual road, rail, water, park and station vectors use OpenStreetMap data retrieved through a fixed Overpass query on 2026-08-18. Attribution is "Map data © OpenStreetMap contributors" and the data is available under ODbL 1.0; retrieval extent and query/response hashes are retained in `sources.json`. This volunteered context is not an official boundary, cadastral layer, road redline, survey, statutory control or engineering basis. No third-party map tile, photograph, logo, or interface asset is used.

PDF text is set in embedded Noto Sans CJK subsets. The two Chinese offline HTML artifacts embed a shared document-specific 153,888-byte WOFF2 subset of Noto Sans SC as a data URL, covering the glyphs used by those exact files. The English proposal HTML embeds a separate navigation-only subset for “阅读中文版本”; its English body keeps system fallbacks. These uses are licensed under the SIL Open Font License 1.1, and the full source font file is not redistributed. The authoritative Noto licence and source reference are recorded in `sources.json`. Any embedded CJK text change requires regenerating the relevant HTML subset, and any later production identity requires a separate font-embedding and name/mark rights review.
