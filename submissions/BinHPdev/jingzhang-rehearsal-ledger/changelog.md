# 方案迭代记录

## v1.1 - 2026-08-25

回应 PR #4006 首轮 AI Agent 评审（request-changes，86.0/100；阻断项为中文 HTML 字体方框）。本轮只补强、不删减；空间图层、指标与演练读数未变。

**修复阻断项（表达完整度）**

- `report/proposal.html`、`report/proposal.en.html`、`visual/index.html`、`visual/index.en.html` **四个 HTML 全部**内嵌
  按本包实际用字（1025 字符）子集化的 Noto Sans SC v2.004（SIL OFL 1.1），固化 400/700 两个字重，
  WOFF2 + base64 写入 `@font-face`，不再依赖评审环境的系统中文字体。采用 `data:` URI 而非外链本地字体文件，
  是因为评审以 `file://` 打开页面，Chromium 对 `file://` 使用不透明来源、字体属 CORS-enabled 请求，
  相对路径字体会被拦截，而仓库禁止远程字体。
- 字体来源、版本、Adobe 版权与保留名、实例化与子集化做法、再分发形式及 **OFL 1.1 全文**
  登记于 `sources.json`（`FONT-NOTO-SANS-SC`）与 `report/copyright_statement.md`；
  按 OFL 第 3 条，CSS 族名取 `JZSansSC`，不使用保留名 `Source`，也不以 `Noto` 名义发布。
- 验证不依赖肉眼判断：对四个 HTML 的全部非 ASCII 字符做 cmap 覆盖检查（缺失 0）；
  在 headless Chromium 中做像素比对——内嵌字形与系统苹方渲染同一串汉字差异 31522 px，
  而苹方与系统默认差异 0 px，证明浏览器采用的是内嵌字形而非系统回退；
  再按评审原参数 `--window-size=1440,1600` 逐页渲染复看，无方框、无乱码、无裁切。
- base64 载荷已用仓库自身的 `FORBIDDEN_HTML_PATTERNS` / `FORBIDDEN_VISUAL_HTML_PATTERNS` 正则回扫，四个文件均干净。
- 原 `report/copyright_statement.md` 中"HTML 只声明字体族名称，不打包、不加载任何字体文件"一句正是本缺陷成因，已改写。

**回应任务书相关性意见（4/5 → 补齐对照闭环）**

- 新增章节「三大定位、五大功能与三区两翼对照闭环」：三大定位逐项落到空间载体、机制、可复算指标与当前缺口；
  五大功能按任务书承载片区逐项给出对应机制与证据；三区两翼写成有交接物的**有向回路**，并如实记录两处尚未闭合。
- 区域协同从一段概念建议改为机制表：北纬社区、未来科学城/怀柔科学城、经开区、京津冀各自的协同内容、交接物与依据限制。
  北纬社区在仓库内无任何定义材料，本方案**不推断其功能定位**，新增 `assumptions.json` A-REGION-006 登记待核实。
- 6 个全球案例由"机制类型"改为**具名、逐源核验**的案例（北京自动驾驶分级管理两份公开文件、加拿大自动化决策指令、NASA ASRS、
  荷兰/赫尔辛基算法登记册、巴塞罗那超级街区、国办发〔2020〕45 号），全部只取制度机制，不引用金额、规模或企业名单；
  9 条来源登记于 `sources.json`，核验日期 2026-08-25。
- `compliance_matrix.json`：agent.1 / agent.2 及 1.3.x、1.5.1.1、1.5.2.1 映射到新增章节与新增来源。
- `proposal.en.md` 同步镜像上述全部改动，证据标记逐一对应。

**未改动**

- `geometry/`、`metrics.json`、`simulation.json`、`risk.json`、`spatial.json` 与全部 PNG / PDF 保持 v1.0 内容不变。
- 可实施性与数据缺口评审意见属于官方几何、制度授权与现场证据的触发型事项，本轮按评审意见保持"待触发后深化"。

## v1.0 - 2026-08-24

首次提交 formal 投稿包。

**方案主张**

- 确立核心机制「准入三件套」：可重放的演练台账、AI-off 等价服务基准、具名的人工复核责任人。
- 三处重点区域按演练等级组织（一级封闭演练场 / 二级生活演练区 / 三级开放门厅），
  一个场景必须逐级通过，不能跳级上街。
- 三处 AI 朝圣地标：台账塔、人字回廊、断电广场。

**证据层**

- `geometry/`：9 个图层由确定性脚本从临时几何推导；用地为完整覆盖、互不重叠的分区，
  覆盖缺口 0 m²；新增 `constraints.geojson` 登记历史线位敏感带与南段位置存疑区。
- `simulation.json`：20 条离线合成演练任务，固定 seed = 20260824。
  **五项验收目标只达成两项**；3 次失败、2 次能耗超预算、2 处审计缺口、1 次协议不合规全部保留。
- `metrics.json`：17 项指标，全部由脚本从已写入磁盘的 GeoJSON 与任务台账复算；
  `floor_area_ratio` 与 `building_height_m` 刻意保持 unknown。
- 新增 `risk.json`（8 维，评分 ≥4 的五项均填写具名人工复核）与 `spatial.json`（8 个概念节点/廊道/区域）。

**主动上报的数据疑点**

- 临时几何中标注为「大钟寺AI产业聚集区」的 `PROV-KEY-003` 质心落在北京北站一带，
  与站名不符（上游 Issue #1029 记录约 2.26 km 偏差）。本方案不使用具体站名指称该片区，
  一律表述为「南段门户」，并在 `constraints.geojson` 中登记位置存疑区。

**待复核事项**

- official boundary 与三处重点区域 official polygons 发布后，全部空间指标、图件、A3/A0 与 HTML 需整体复算。
- 场景准入分级机制没有现成法定依据，需要单独制度授权（`risk.json` 中政策不确定性评为 5 分）。
- 本轮未达成的三项验收目标（协议合规率、审计完整率、能耗超预算）需写入下一轮任务清单。
